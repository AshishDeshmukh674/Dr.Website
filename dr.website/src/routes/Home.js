import React from 'react';
import FirstPage from '../components/Firstpage'; // Ensure Firstpage is correctly named
import ContactUs from '../components/contactus'; // Ensure Contactus is correctly named

const Home = () => {
  return (
    <div>
      <FirstPage />
      <ContactUs />
    </div>
  );
}

export default Home;
